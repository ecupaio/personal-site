const project=document.querySelectorAll(".project-col"),projectOverlay=document.querySelector(".project-overlay");project.forEach(item=>{item.addEventListener("click",event=>{projectOverlay.classList.add("active");const projectTitle=event.target.querySelector(".project-title").innerText;console.log(projectTitle),fetch("/projects.json").then(response=>response.json()).then(projects=>{projects.find(({title:title})=>title===projectTitle).images.forEach(image=>{});new Swiper(".swiper",{direction:"horizontal",loop:!0,navigation:{nextEl:".swiper-button-next",prevEl:".swiper-button-prev"}})}).catch(err=>console.log(err))})});